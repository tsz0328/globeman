# 前端架构评审报告 · quanqiuren-web

> 评审专家：鹏城信息AI专家（前端架构分析） ｜ 评审日期：2026-08-14 ｜ 评审方式：静态分析 + 经验规则
> 项目定位：企业资产/订单/维修/项目管理系统前端（Vue 3 + TypeScript + Element Plus + Vite）

---

## 一、项目信息

| 项 | 内容 |
|---|---|
| 主框架 | Vue 3.5.30（Composition API，`<script setup>` 覆盖率 100%） |
| 语言 | TypeScript 5.9.3（`strict` + `noUncheckedIndexedAccess`） |
| 构建 | Vite 7.3.1（产物目录 `static/`，非默认 `dist/`） |
| UI 库 | Element Plus 2.13.6 + @element-plus/icons-vue |
| 状态 | Pinia 3.0.4（已挂载，**实际未创建任何 store**） |
| 路由 | vue-router 4.6.4 |
| HTTP | axios 1.14.0（统一 `request.ts` 拦截器） |
| 规模 | 72 个源文件 / 14,070 行（.vue 均 259 行，.ts 均 107 行） |
| 质量工具链 | ESLint 10 + oxlint + Prettier + EditorConfig；vue-tsc 类型检查 |
| 测试 | Vitest 已配置，**0 个 spec 文件（无自动化测试）** |
| 依赖管理 | 存在 `package-lock.json`（无幽灵依赖风险） |

---

## 二、总览评分

| 维度 | 得分 | 满分 | 诊断 |
|---|---:|---:|---|
| 技术栈健康度 | 42 | 50 | 优秀且现代，仅 Pinia 挂而未用 |
| 架构设计模式 | 30 | 50 | 分层清晰，但存在上帝组件与大量重复 |
| 工程化成熟度 | 32 | 50 | Lint/类型检查完备，缺测试与 CI |
| 性能与可维护性 | 28 | 50 | 无路由懒加载、巨型组件拖累 |
| **综合** | **132 / 200 → 66 / 100** | | **⭐⭐⭐（三星）** |

**结论**：技术选型与工程规范基础扎实，是一套"能跑、能维护、但离工程化标杆还有距离"的中小型业务前端。短板集中在**组件粒度过大、分页/筛选逻辑重复、零自动化测试、无代码分割**。

---

## 三、五个维度详解

### 1. 技术栈健康度 · 42/50
**优点**
- Vue 3.5 / Vite 7 / TS 5.9 / Element Plus 2.13 全部为当前主流稳定版本，无 EOL 风险。
- TypeScript 开启 `strict` 且额外开启 `noUncheckedIndexedAccess`（较激进的严格度），从 `vue-tsc --noEmit` 长期 EXIT=0 看，类型纪律执行到位。
- 100% `<script setup>`，无 Options API 混杂；目录按业务域清晰拆分（`api/`、`composables/`、`views/`、`components/`）。
- `package-lock.json` 锁定，依赖可复现。

**扣分点**
- `pinia` 已 `app.use(createPinia())`，但全仓 `0` 个 `defineStore`、无 `src/stores`——装了却没用，属于"配置与实现脱节"。
- `vite.config.ts` 产物目录为 `static/`（非 `dist/`），与社区惯例不符，已导致一次"误删构建产物目录"的事故（见记忆记录），应改回 `dist/` 或明确文档化。

### 2. 架构设计模式 · 30/50
**优点**
- API 层按域拆分（`admin/asset/order/project/repair`），每接口配套 `interface` 类型定义，`request.ts` 拦截器对鉴权失效、错误归一化、空响应处理都较完善（含并发过期去重跳转，质量高）。
- 组件内状态用 `composables/*` 收敛（如 `useUser`/`useOrder`），避免了全量堆在组件里。

**扣分点（核心问题）**
- **上帝组件**：`OrderDetailDialog.vue` 775 行、`AddOrderForm.vue` 714 行、`EquipmentRepairInfo.vue` 593 行、`RepairOrderDetail.vue` 483 行、`ProjectOrderManagement.vue` 462 行。单文件超过 500 行，可读性与单测难度陡增。
- **DRY 严重 violation**：至少 **12 个管理页** 各自手写 `filteredData` + `paginatedData` 两个 computed（筛选 + 切片分页逻辑完全相同），应抽出 `useTableQuery` 通用组合式。
- **鉴权/角色分散**：`role` 直接 `Cookies.get('role')` 在各组件内读取，而非集中到 auth store；路由守卫 `beforeEach` 仅校验 token 是否存在，未做角色级路由守卫（服务端仍是最终防线，但前端体验与安全性偏弱）。
- **接口契约不一致**：后端部分接口直接返回数组、部分返回 `{code,data}`、资产列表返回嵌套 `{number,total,assets}`，`request.ts` 被迫多分支兼容——是后端契约不统一的信号。

### 3. 工程化成熟度 · 32/50
**优点**
- 双 Lint（ESLint + oxlint）、Prettier、EditorConfig 齐全，`npm run lint` 与 `type-check` 在脚本中。
- 类型检查已作为改动验收标准（团队习惯良好）。

**扣分点**
- **零自动化测试**：Vitest 配置齐备但无 1 个 spec；Playwright 已被移除，仅依赖手动 E2E + Network 面板。关键路径（订单创建、资产 CRUD、拦截器）无任何回归保护。
- **无 CI 配置**：仓库根未发现 `.github/workflows` 等流水线，提交即上线风险高。
- 部分接口仍为推断 TODO（`updateAssets`/`scrapAssets` 地址/方法未知），与后端契约未完全对齐，属协作层面的工程债。

### 4. 性能与可维护性 · 28/50
**优点**
- 列表统一"后端全量返回 + 前端切片分页（pageSize=10）"策略，模式一致，无服务端分页耦合。
- 组件按需 `import` 公共部分组件，无明显内存泄漏迹象。

**扣分点（性能主因）**
- **无路由懒加载**：`router/index.ts` 中 30+ 路由组件**全部静态 import**，无 `() => import()`、无 `defineAsyncComponent`。首屏即加载全部 14k 行代码，初始包体偏大，弱网/首屏体验受损。
- 巨型组件 + 重复逻辑使"改一处"成本高、易回归，长期可维护性走低。
- `OrderDetailDialog.vue` 用 `document.body.innerHTML` 做打印（602–605 行）属脆弱写法，建议改用 `@media print` 或隐藏 iframe 打印。

---

## 四、重构优先级（P0–P3）

| 优先级 | 改进项 | 预期收益 | 估算工时 | 建议动作 |
|---|---|---|---|---|
| P1 | 路由懒加载 | 首屏包体显著下降、TTI 提升 | 0.5d | 将 `router/index.ts` 静态 import 改为 `() => import('@/views/...')` |
| P1 | 抽出 `useTableQuery` 组合式 | 消除 12 处重复筛选/分页，降低回归 | 1d | 入参=原始数组+筛选表单，返回 `filteredList`/`pagedList`/`handleSearch`/`handleReset` |
| P1 | 拆分上帝组件 | 可维护性、可测试性提升 | 2–3d | 将 `OrderDetailDialog`/`AddOrderForm` 等拆为"页 + 子区块组件"，SN 子表、设备明细表独立成组件 |
| P2 | 引入 Pinia auth store | 角色/权限/用户信息集中，去 Cookie 散读 | 1d | 建 `stores/auth.ts`，路由守卫做角色级 `meta.roles` 校验 |
| P2 | 补齐关键路径单测 | 防回归（尤其拦截器、订单/资产逻辑） | 2d | 优先测 `request.ts`、各 `Api.ts` mock、1–2 个核心 composable |
| P2 | 接入 CI | 提交即类型检查+lint+测试 | 0.5d | 加 GitHub Actions 跑 `type-check`+`lint`+`test:unit` |
| P3 | 产物目录改 `dist/` | 避免误删、契合惯例 | 0.5h | 改 `vite.config.ts` `outDir`，同步 nginx 配置 |
| P3 | 清理技术债 | 减少误导 | 0.5d | 删除 `AssetApi` 中"列表无 id"等陈旧注释；推动 `updateAssets`/`scrapAssets` 与后端对齐 |
| P3 | 打印逻辑改造 | 去除 `innerHTML` 脆弱写法 | 0.5d | 改用 `@media print` 或隐藏 iframe |

> 估算工时为单人日经验值，实际随团队节奏浮动。

---

## 五、免责声明

本报告基于静态代码分析（目录结构、配置文件、源码规模与模式扫描）与前端架构经验规则生成，**仅供参考**。评分反映的是"相对工程化标杆"的位置，不构成唯一正确决策；架构没有银弹，合适的才是最好的。实际重构优先级请结合业务演进、团队规模与排期综合判断。未包含生产环境运行时 profiling、真实网络与包体体积数据，相关结论为定性推断。
